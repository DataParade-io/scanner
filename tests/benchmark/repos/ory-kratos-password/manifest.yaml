repository: ory/kratos
commit: b86338da04a040247a07f46100a86dcfb3875909
license: Apache-2.0
scope:
  include:
    - identity/credentials_password.go
coverage:
  layers:
    - data_items
    - mentions
    - components
    - data_flows
  languages:
    - go
  domains:
    - identity
    - credentials
    - password
selection_rationale: A complete 21-line authored Go type explicitly distinguishes a hashed password from password-migration control metadata. It adds password-verifier semantics distinct from bearer access tokens and generic credential payloads.
annotation_version: 2
