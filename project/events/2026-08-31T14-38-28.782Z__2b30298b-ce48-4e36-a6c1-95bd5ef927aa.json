{
  "schema_version": 1,
  "event_id": "2b30298b-ce48-4e36-a6c1-95bd5ef927aa",
  "issue_id": "KDATAP-00e64acd-d680-4fc7-949f-7e51e0c6c8c2",
  "event_type": "field_updated",
  "occurred_at": "2026-08-31T14:38:28.782Z",
  "actor_id": "home",
  "payload": {
    "changes": {
      "description": {
        "from": "KDATAP-08a0ac requires \"canonical instance/vendor when known\" plus \"one strict all-required-fields result\". The migration reading of \"when known\" decides whether the component number is usable or structurally zero.\n\nAll 563 component annotations carry a `subject.name`. For the 473 assets it is a code-level instance (`directus_users`, `LocalAuthDriver`, `UsersService`). If the migration reads those as the canonical instance field, 473 expectations acquire an instance requirement and the strict result stays near zero for exactly the 85% of gold that is otherwise satisfiable — the failure the decision exists to prevent, reintroduced by a plausible migration step.\n\nMeasured shape of component gold:\n\n| gold key shape | instances | share |\n| --- | ---: | ---: |\n| `type:<taxonomy subtype id>` | 481 | 85% |\n| `third_party:<vendor>` | 52 | 9% |\n| `actor:user` (no declared subtype) | 30 | 5% |\n\n**Proposed decision.** Instance is a required field only where gold asserts a vendor — the 52 `third_party:` cases. For assets and actors the strict result is (type, subtype), which is the whole claim those annotations make. Instance/vendor resolution is graded as its own diagnostic over the population that actually claims one.\n\n**Corollary to state in the contract:** strict means all fields *gold asserts*, not all fields the schema defines. Otherwise \"when known\" gets implemented as \"always required\".\n\nRejected: treating asset `subject.name` as the canonical instance; requiring every schema field on every expectation.",
        "to": "KDATAP-08a0ac requires canonical instance/vendor when asserted plus one strict all-required-fields result. The migration reading decides whether component scoring is meaningful.\n\nAll 563 legacy component annotations carry `subject.name`. For 473 assets it is a code-level value; automatically requiring it would make strict recall structurally near zero. For 52 third-party rows the legacy key and name genuinely assert a vendor.\n\n**Proposed decision.**\n\n- Strict means all fields the gold record asserts, not every field the schema defines.\n- Vendor/instance is required only for records whose reviewed canonical gold asserts it.\n- Asset and actor display/code names are preserved as evidence candidates but are not automatically asserted instances.\n- Vendor-resolution metrics use their own denominator over records that assert a vendor, so 52 vendor cases cannot disappear inside 481 subtype-only cases.\n- Repo-local component entity/cardinality is a separate ruling; optional instance must not be abused to distinguish repeated same-subtype entities.\n\nRejected: every schema field required; every legacy name promoted to instance; vendor misses hidden inside a broad subtype score."
      }
    }
  }
}